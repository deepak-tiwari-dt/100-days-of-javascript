function addition(num) {
	return num + 1;
}

const addition = (num) => {
  return num + 1;
}

const addition = (num) => num + 1;

const addition = function(num) {
  return num + 1;
}

const addition = num => num + 1;
